{
  "name": "jirassic-parity",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Differential oracle: capture real Jira + Jirassic Park, normalize, diff. Ranger closes the loop: detect (Opus) -> patch (Cursor SDK) -> judge.",
  "scripts": {
    "login": "tsx src/login.ts",
    "run": "tsx src/run.ts",
    "ranger": "tsx src/ranger.ts",
    "ranger:fixtures": "tsx src/ranger.ts --capture-fixtures",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.100.0",
    "@cursor/sdk": "^1.0.15",
    "dotenv": "^17.4.2",
    "playwright": "^1.48.0",
    "pixelmatch": "^6.0.0",
    "pngjs": "^7.0.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "@types/node": "^20.16.0",
    "@types/pngjs": "^6.0.5"
  }
}
